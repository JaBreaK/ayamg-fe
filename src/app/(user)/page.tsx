// src/app/(user)/page.tsx

import LandingPageClient from '@/components/LandingPageClient'; // Impor komponen klien

// 1. Sediakan Metadata (Server-side)

// 2. Tampilkan Komponen Klien
export default function HomePage() {
  return <LandingPageClient />;
}
